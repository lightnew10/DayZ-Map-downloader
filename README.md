# Prerequisites
- Python 3.10 or higher
# Installation
1. Set up a Virtual Environment (venv):
`python3 -m venv venv`
2. Activate the environment
3. Install dependencies:
`pip install requests`

# Modification map
Just go in main.py and change name of the map or follow instructions in main.py

and its DONE :)
